# Record

## 记录格式

每条记录采用以下格式：

```markdown
## 版本号 短语说明做了什么

用 1～3 句话说明具体完成了什么、验证结果，以及必要的下一步。
```

版本号规则：

- `1.0`、`2.0`：完成一个主要阶段
- `1.1`、`1.2`：完成阶段内的一个小步骤
- 只记录已经实际完成的内容

## 1.0 建立项目规划

创建了 `Create GPT Step by Step` 项目，并制定了从字符级 tokenizer 到聊天模型扩展的学习路线。建立项目文档和本地 Git 仓库，并把首次提交推送到公开 GitHub 仓库。安装 Python 3.13.15，创建独立虚拟环境，安装 PyTorch 2.13.0 和 NumPy 2.5.2，并验证 MPS GPU 可用。

## 1.1 实现字符级 Tokenizer

读取包含 153 个字符的训练文本，建立了包含 92 个不同字符的 `stoi` 和 `itos` 双向映射。实现 `encode()` 与 `decode()`，并通过测试句和全文的无损往返测试。

## 1.2 构造训练数据

将全文编码为 `torch.long` 张量，并按 90%/10% 切分为训练集和验证集。使用长度为 8 的上下文窗口构造错开一个 token 的输入与目标，实现随机抽样及形状为 `[4, 8]` 的训练和验证 batch。

## 2.0 训练 Bigram 基线模型

建立形状为 `[92, 92]`、共 8464 个参数的 Bigram 预测表，完成 Logits、交叉熵、AdamW 参数更新和 1000 步训练，平均训练 Loss 降至约 0.71。实现从字符“小”开始的连续 100 Token 生成；平均验证 Loss 约为 4.89，观察到小数据集上的明显过拟合，也验证了 Bigram 只能参考前一个字符的能力限制。

## 3.0 实现因果 Self-Attention

把 Token Embedding 与 Position Embedding 合并为形状 `[4, 8, 32]` 的上下文输入，手动实现 Query、Key、Value、缩放点积、因果遮罩和 Value 加权汇总。将单头注意力扩展为 4 个并行 Head，验证每个 Head 输出 `[4, 8, 8]`、拼接与投影后输出 `[4, 8, 32]`，且未来 Token 的注意力概率为 0。

## 4.0 组合 Transformer Block

封装 `MultiHeadAttention`、`FeedForward` 和 Pre-Norm `TransformerBlock`，加入两次 LayerNorm、输出投影和两条残差连接。成功堆叠两个 Block，验证输入输出均为 `[4, 8, 32]`，两个 Block 的全部参数都获得非零梯度，堆栈参数总数为 25216。

## 5.0 组合完整 GPT 模型

将 Token Embedding、Position Embedding、两个 Transformer Block、最终 LayerNorm 和语言模型输出层封装为 `GPTLanguageModel`。验证输入 `[4, 8]` 能输出 `[4, 8, 92]` 的 Logits，初始交叉熵 Loss 约为 4.78，完整模型参数总数为 31516。

## 5.1 完成语料机械清洗

备份并隔离《斗破苍穹》原始语料，建立不会提交到 GitHub 的工作副本。第一阶段清洗删除了 11220 个 NUL 空字符并统一了 198068 个 CRLF 换行，输出可正常按 UTF-8 解码且没有替换字符；同时生成 JSON 报告和可独立调节的 I/O、文本变换轮换日志。

## 5.2 审计语料内容污染

新增只读内容审计脚本，在不删除正文的前提下识别来源声明、下载宣传、阅读站水印、更新提示和电子书广告。共发现 81 行候选污染，其中 30 行可整行处理、51 行广告嵌在正文中；同时记录了 1291 组重复章节标题和 20 个未出现的章节编号，供下一步保守清洗与重复内容核对。

## 5.3 审计重复章节与正文

使用章节边界指纹和连续非空段落匹配检查重复内容，在 1903 个章节区块中没有发现边界完全一致的整章副本，但发现 105 处至少连续 5 段、500 字以上的完全重复正文。105 个较晚出现的重复范围互不重叠，保守估算包含 117153 个冗余字符；抽样确认重复来自同一章节的不同抓取版本，而非正常剧情复述。

## 5.4 清除网页广告与正文水印

第二阶段清洗删除了 57 行来源声明、网页框架、导航和电子书广告，并从正文中精确移除了 51 处网站水印和更新提示，共减少 3155 个字符、保留原文的 99.9524%。独立复查对现有污染规则的残留命中为 0，四项测试确认整行广告、嵌入水印和更新横幅能够删除，同时普通正文保持不变。

## 5.5 配对近似重复章节版本

将同章节编号以及重复正文块对应到 1903 个章节区块，共比较 316 组候选并发现 135 组相似版本。采用“章节编号相同且较短版本至少 98% 被另一版覆盖”的保守标准，得到 22 组无建议冲突的高置信版本，另外 113 组保留复核；测试确认跨章节相似内容不会被标记为自动删除对象。

## 5.6 剔除高置信重复版本

在保留 stage2 清洗结果的前提下，仅根据 `chapter_version_audit.json` 的 high 置信结果，移除了 22 个重复章节版本。共清理 2563 行、76711 字符，保留率约 98.84%，并生成 `data/clean/doupo_stage3.txt` 与 `data/clean/stage3_report.json`，每段删除范围都带有章节编号与标题。

为了确保安全性，删除过程保持了章节边界完整性，并将输入/输出 SHA256 与字符数写入报告，便于下一步回审。

## 5.7 复审 Stage3 语料

对 `doupo_stage3.txt` 进行三项复审：内容污染、重复正文块、章节版本配对。复审结果为候选污染行 0、章节区块数 1881、重复正文块 101、版本配对保留 113 条但全部为 review，high 置信为 0。对应报告分别是 `content_audit_stage3.json`、`duplicate_audit_stage3.json`、`chapter_version_audit_stage3.json`。

此时语料进入“可开始重新编码训练集/验证集”的状态。

## 5.8 构建 stage3 训练数据

基于 `data/clean/doupo_stage3.txt` 生成字符级 tokenizer 张量，统计词表到 4478 个字符，产出 `data/clean/doupo_stage3_tensors.pt`。按 90/10 划分后得到训练集 5889850、验证集 654428 token（总约 6544278），并输出 `stage3_dataset_report.json` 及 `stage3_vocab.json` 供后续训练复现。

## 5.9 训练输入 Embedding 步骤

新增 `embedding_step.py`，将训练 batch 的 token ids 映射到 `token_embedding`，并添加 `position_embedding` 形成 `token + position` 的上下文向量。通过 `doupo_stage3_tensors.pt` 验证了输出形状：token 形状 `[4, 8]`、token 向量 `[4, 8, 64]`、位置向量 `[8, 64]`、融合后上下文向量 `[4, 8, 64]`，并生成 `embedding_report.json` 记录本步统计。

## 5.10 扩大训练批次与上下文并试跑 6.0 训练循环

新增 `train_gpt_stage3.py`，复用 `Transformer Block` 结构实现完整 GPT 训练前向与交叉熵损失。用 `BLOCK_SIZE=64`、`BATCH_SIZE=16` 跑了 80 步（含定期评估）后，训练损失从约 8.56 降到 6.69，验证损失同步下降到约 6.66，证明模型开始学到上下文规律。

脚本同时加入分模块日志、梯度裁剪和 checkpoint 导出（含 loss 历史），将第一批输出和参数配置记录到 `checkpoints/gpt_stage3_history.json`，下一步可继续增大 `NUM_HEADS/NUM_LAYERS` 或步数。

## 5.11 训练脚本升级为续训与生成

把 `train_gpt_stage3.py` 再升级为可控训练工具：支持 `RESUME_TRAINING=1` 从上次 checkpoint 继续训练，支持 `GEN_STEPS/GEN_START_TEXT/TOP_K/TEMPERATURE` 进行采样生成，支持自定义 checkpoint 路径。通过 10 步测试验证续训从 step 10 成功接续到 step 15，且可直接输出生成片段。

## 5.12 增加可复现训练能力与参数固定

在 `train_gpt_stage3.py` 中加入 `SEED` 与 `DETERMINISTIC` 两个开关，统一设置 torch 的随机种子（CPU 与 CUDA）并写入日志/Checkpoint 元信息，便于同一环境下复现实验轨迹。并验证了 1~5 步短跑运行可正常完成并输出 loss 曲线，当前默认已可用于后续“固定参数稳定对比”的阶段。

## 5.13 续训到 step 120 并观察 loss 降幅

基于 `checkpoints/gpt_stage3.pt` 执行续训（`RESUME_TRAINING=1`，`MAX_STEPS=120`，`BATCH_SIZE=4`，`BLOCK_SIZE=64`，`SEED=42`）并记录日志。loss 在训练集/验证集上均持续下降，约从 `step 20: 8.35/8.33` 到 `step 120: 6.35/6.35`，表明模型继续拟合语料并具备稳定学习趋势。并生成 sample 与续写文本用于检视输出质量。

## 5.14 固定 10 句 prompt 的训练后对比评估

新增 `data/prompt10_eval.txt`（10 条固定中文 prompt）并在训练中加入 `PROMPT_EVAL_*` 开关：按 `PROMPT_EVAL_EVERY=20` 在 step 140、160 采集生成结果。通过 `checkpoints/prompt_eval_stage3.json` 保存逐步输出，验证后续同一组输入在不同训练阶段的文风变化，当前样本中可见输出仍有重复噪声，训练继续改进空间。

## 5.15 续训至 step 10000 并复测固定样本

使用 `BATCH_SIZE=16`、`BLOCK_SIZE=64`、`EMBEDDING_DIM=64`、4 个注意力头和 2 个 Transformer Block，将 stage3 模型续训至 step 10000。最终平均训练 Loss 为 3.7468，平均验证 Loss 为 3.8638，两者差距约 0.117，尚未出现明显的训练集过拟合分叉。

加载 `checkpoints/gpt_stage3.pt`，在不更新参数的情况下，用固定 10 条 prompt、温度 1.0、Top-K 40 和每题 80 个新字符进行复测。输出已经明显具有《斗破苍穹》的角色、标点和叙事风格，但仍无法稳定理解问题、遵循“三句话”等指令或保持长程语义连贯；当前模型属于小说字符级续写模型，还不是问答聊天模型。

## 5.16 对比三组文本采样参数

固定 step 10000 模型、10 条 prompt、随机种子 42 和每题最多 30 个新字符，对比了保守采样（Temperature 0.5、Top-K 10）、均衡采样（0.8、20）和自由采样（1.0、40）。保守组更容易出现连续语气词和称呼重复，自由组的语义跳跃及重复同样明显；均衡组相对减少了极端重复和随机跳跃，因此暂定 `Temperature=0.8`、`Top-K=20` 为当前 stage3 模型的推荐生成参数。

本次对比也确认：采样参数只能调节随机性和重复程度，无法解决模型不理解问题的问题。下一阶段需要扩大上下文与模型容量，或在后续加入指令问答数据。

## 5.17 建立 Stage 4 扩容模型并完成 100 步试跑

保留 Stage 3 检查点不变，新建 Stage 4 模型：上下文长度由 64 扩大到 128，Embedding 维度由 64 扩大到 128，Transformer Block 由 2 层增加到 4 层，注意力头维持 4 个，Batch Size 设为 8。可训练参数从 Stage 3 的 714238 增加到 1959038；前向输出形状为 `[8, 128, 4478]`，并确认全部可训练参数均能获得梯度。

使用 CPU 从零完成 step 0~100 smoke test，每 20 步评估一次。训练 Loss 从 8.5259 降至 5.8286，验证 Loss 从 8.5384 降至 5.6711，训练过程无报错且未出现验证 Loss 分叉。模型已保存为 `checkpoints/gpt_stage4.pt`，Stage 3 的 `checkpoints/gpt_stage3.pt` 未被覆盖；100 步生成文本仍不连贯，符合新模型训练步数很少的预期。

## 5.18 实测 Mac 容量并调整为 Stage 5

在 M4、16 GB MacBook Air 上依次测试约 196 万、710 万、1773 万、2993 万、7794 万和 1.10 亿可训练参数的模型。每组均完成前向传播、Loss、反向传播、梯度裁剪和 AdamW 更新；1.10 亿参数组能够运行，但当前 PyTorch 环境无法启用 MPS，全部测试使用 CPU。完整结果记录在 `reports/mac_capacity_benchmark.md`。

结合约 654 万字符的训练语料、训练速度和过拟合风险，将推荐配置调整为 Stage 5：`BATCH_SIZE=4`、`BLOCK_SIZE=256`、`EMBEDDING_DIM=256`、`NUM_HEADS=8`、`NUM_LAYERS=6`，共 7097214 个可训练参数。Stage 5 从零完成 100 步试跑，训练 Loss 从 8.3947 降至 5.3452，验证 Loss 从 8.3922 降至 5.3014，并保存为 `checkpoints/gpt_stage5.pt`；Stage 3 和 Stage 4 检查点均保留。

## 5.19 Stage 5 续训至 step 500

从 `checkpoints/gpt_stage5.pt` 的 step 100 继续训练至 step 500，保持 Batch 4、上下文 256、Embedding 256、8 个注意力头、6 层 Transformer 和学习率 `3e-4` 不变。训练 Loss 从 step 200 的 4.8291 继续降至 step 500 的 4.3426，验证 Loss 从 4.8481 降至 4.3749；最终训练/验证差距约 0.0322，尚未出现明显过拟合。

在 step 500 使用固定 10 条 prompt、每题最多 30 个字符、Temperature 0.8 和 Top-K 20 完成评测，结果保存为 `checkpoints/prompt_eval_stage5.json`。文本已开始出现较完整的小说片段，如“萧炎心中一眼起的一声”，但仍不能稳定回答问题，符合基础小说语言模型在早期训练阶段的表现。

## 5.20 Stage 5 续训至 step 5000

从 step 500 继续训练至 step 5000，每 500 步记录训练/验证 Loss，并使用固定 10 条 prompt、每题最多 30 个字符、Temperature 0.8、Top-K 20 生成阶段样本。step 5000 的20-Batch估计为训练 Loss 3.1407、验证 Loss 3.3964；使用100个 Batch 复核后为 3.1704/3.3448，差距约 0.1745。

模型已经能较稳定地产生小说式人物、动作、对话和标点结构，例如“萧炎沉默的道。一名老者微微点头”，但仍不具备问题理解或指令遵循能力。训练后期训练/验证差距开始扩大，后续需要增加按验证 Loss 保存最佳 checkpoint 的机制，避免只保存最后一步模型。step 5000 模型保存在 `checkpoints/gpt_stage5.pt`，每500步的固定 prompt 结果保存在 `checkpoints/prompt_eval_stage5_to5000.json`。

## 5.21 增加最佳验证模型自动保存

在训练脚本中加入独立的最佳 checkpoint 路径和保存逻辑：每次验证后将当前 Loss 与历史最佳值比较，只有取得更低验证 Loss 时才覆盖 `{prefix}_best.pt`；普通 `{prefix}.pt` 仍保存最后一步。检查点元信息新增 `best_val_loss`，日志分别记录最佳模型创建、改善覆盖、未改善跳过和保存失败，便于独立诊断。

新增 `tests/test_best_checkpoint.py`，覆盖首次创建、较差结果不覆盖和更好结果覆盖三条路径；项目全部 13 项测试通过，并用隔离的2步训练验证了真实文件与日志。由于旧流程没有保存 step 4000 权重，现以 step 5000 的100-Batch验证 Loss 3.344846 作为现存最佳基线，生成 `checkpoints/gpt_stage5_best.pt`；后续只有更低的验证 Loss 才会替换它。

## 5.22 Stage 5 续训至 step 10000

从 step 5000 继续训练至 step 10000，保持约 710 万参数的 Stage 5 结构、学习率 `3e-4`、每 500 步评估和固定 10 条 prompt 不变。最佳验证 Loss 依次在 step 5500、6000、7000、7500、8000、8500 和 10000 刷新；step 6500、9000、9500 未改善时，日志确认最佳 checkpoint 没有被覆盖。

step 10000 的20-Batch训练/验证 Loss 为 2.8335/3.0033，并将 `checkpoints/gpt_stage5_best.pt` 更新到 step 10000。随后使用100个 Batch 复核得到训练 Loss 2.8302、验证 Loss 3.0799、差距约 0.2496；差距表明模型开始更明显地拟合训练语料，但验证 Loss 仍较 step 5000 总体下降。最后模型保存在 `checkpoints/gpt_stage5.pt`，阶段样本保存在 `checkpoints/prompt_eval_stage5_5000_to10000.json`。

## 5.23 增加完整模型指标与 Loss 可视化

新增 `evaluate_stage5.py`，使用最佳 checkpoint 在100个验证 Batch、102400个字符预测上计算交叉熵、Top-1 和 Top-5 准确率，并对固定10条生成样本计算相邻字符、二元组和三元组重复率。正式结果为验证 Loss 3.0863、Top-1 42.71%、Top-5 62.86%；排除空格与换行后，三种平均重复率分别为 1.30%、2.59% 和 0.36%。

根据29个历史评估点生成 `reports/stage5_loss_curve.png` 与 SVG 版本，使用颜色和实线/虚线共同区分训练及验证曲线，标注 step 10000 最佳点，并提供 `stage5_loss_table.csv` 数据表和图表替代文本。评测、生成、数据与图表日志可分别过滤；新增5项指标及图表测试后，项目全部18项测试通过。

## 5.24 建立视频素材里程碑并确定 SFT v1 路线

建立 `VIDEO_MATERIALS.md` 作为项目视频素材总入口，规定每次实质优化都保留独立 checkpoint、配置、指标、固定样本、曲线、结论和 SHA-256。将 Stage 5 Step 10000 归档为 M001，模型复制为 `checkpoints/archive/gpt_stage5_pretrain_step10000_best.pt`，指标、Loss图表、CSV与每500步样本归档到 `reports/milestones/001_pretrain_stage5_step10000/`。

路线决定为停止继续堆叠当前字符级小说预训练步数，下一阶段直接在当前最佳模型上执行一次字符级 SFT v1，以固定问题展示预训练与指令微调的行为差异。该实验完成后再进入 BPE v2 路线，重新比较字符级与 BPE 模型的训练效率和 SFT 效果。

## 5.25 建立带原文证据的 SFT 种子数据集

建立 M002 字符级 SFT 数据集 v1，共 16 条可核验问答，其中训练集 12 条、验证集 4 条。每条问答都保存原文证据、原文行号和知识主题；准备脚本会逐条检查证据确实出现在授权语料的指定行中，并阻止同一主题同时进入训练集和验证集。

在原 4478 字符词表后增加 `<BOS>`、`<USER>`、`<ASSISTANT>`、`<EOS>`、`<PAD>` 五个完整特殊 Token，扩展词表为 4483。序列格式为 `<BOS><USER>问题<ASSISTANT>回答<EOS>`，问题部分标签设为 `-100`，仅回答和 `<EOS>` 参与交叉熵 Loss。新增分模块轮转日志与 5 项自动化测试，为下一阶段从 M001 checkpoint 开始 SFT 做好数据基础。

## 5.26 扩展为50条蒸馏式 SFT 试验数据

保留 M002 的16条种子集及哈希不变，另建 `sft_pilot50_v1`。数据包含40条训练、5条验证和5条测试问答，覆盖人物关系、炼药师、斗技、学院、异火与世界设定；测试记录独立导出，SFT 期间不得用于 Loss 或参数更新。

问答由教师模型依据授权原文整理，属于带原文约束的数据蒸馏。准备程序逐条验证证据与指定原文行完全对应、问题与答案字符均在词表内、切分数量正确、问题无重复且知识主题不跨集合泄漏。该批数据用于跑通 SFT 和制作训练前后对比，验证有效后再分批扩展到200至500条。

## 5.27 保存特殊 Token 扩展后的 SFT 前问答基线

从归档的 Stage 5 Step 10000 最佳预训练 checkpoint 初始化 SFT 模型，将词表由4478扩展到4483。原有 Embedding、Transformer 与输出层参数逐项复制并验证完全一致，只随机初始化5个特殊 Token 对应的2565个新参数；总参数由7097214增加到7099779。初始化 checkpoint 独立保存为 `checkpoints/archive/sft_stage1_init_pre_sft.pt`，其中 `sft_steps=0`，未计算训练 Loss、未反向传播、未更新参数。

使用5道隔离测试题、最多30字、Temperature 0.8、Top-K 20和固定随机种子记录 SFT 前完整输出。结果 Exact Match 为0/5、答案包含率为0/5、`<EOS>`主动停止率为0/5；所有回答仍表现为小说续写。结果归档在 M002b，后续 SFT 必须使用同一脚本与生成配置复测。

## 5.28 完成20步 SFT 安全试跑

使用40条训练、5条验证问答从 SFT 前初始化 checkpoint 训练20步，Batch Size 4、学习率 `1e-4`、Weight Decay `0.01`、梯度裁剪 `1.0`。训练脚本只加载训练和验证字段，5条固定测试题未进入训练或调参，报告记录 `test_records_consumed=0`。

完整训练 Loss 从4.4718降至1.9940，验证 Loss 从4.4909降至3.4976；第5、10、15、20步均刷新最佳验证模型。每一步均产生有限的梯度并完成裁剪与参数更新，证明回答部分标签、交叉熵、反向传播和 checkpoint 流程全部跑通。训练样本首次学会主动输出 `<EOS>`，但验证样本仍然无关，且训练/验证差距约1.50，说明小数据很容易被记忆，后续应谨慎增加步数并扩充数据。

## 5.29 将 SFT 数据分批扩充到100条

保留原50条试验数据和5道固定测试题完全不变，新增50条带原文证据的蒸馏式问答。组合数据现为80条训练、15条验证、5条测试，共100个互不重复的知识主题；新增内容覆盖修炼等级、炼药条件、魔核用途、魔兽等级、飞行斗技、紫云翼、玄重尺和焰分噬浪尺。

全部100条记录通过原文行证据、词表字符、ID与问题重复、切分数量和跨集合主题泄漏检查。最长序列50个 Token，屏蔽标签1528个，回答与 `<EOS>` 监督标签969个，扩展词表仍为4483。下一次训练将重新从 SFT 前初始化 checkpoint 开始，使数据量成为40条试跑与80条训练实验之间的主要变量。

## 5.30 将 SFT 指令数据扩充到500条

完整保留 M003b 的100条证据问答与5道固定事实测试题，新增400条从授权清洗语料确定性生成的原文补全指令。最终数据为400条训练、75条验证和25条测试；新增部分按320/60/20切分，并使用4种提示语各100条，训练模型识别指令、进入助手回答区和学习 `<EOS>` 停止。

新增400条分别来自400个独立原文行，证据均能在指定行精确找到，没有新增训练、验证、测试来源行交叉。校验同时发现旧100条在原文第378行存在一处历史训练/验证共同来源，本阶段为保持已有实验可比性而保留，并在报告中明确披露。500个问题和500个主题均唯一，词表仍为4483；全项目42项测试通过。

本阶段不把数量包装成质量：新增400条主要是原文补全指令，并非400条人工审核的事实问答。它适合做“数据量与指令格式”实验，但后续仍需补充更高质量的直接问答、解释型回答和拒答数据。下一次训练必须从同一份 SFT 前初始化 checkpoint 重新开始，避免把20步试跑的已有更新混入对照。

## 5.31 构造高质量1000条SFT数据集

建立 `sft_hq1000_v2`，保留原100条证据事实问答，新增900条干净概念分类问答，最终切分为800条训练、100条验证和100条测试。新增数据覆盖150个独立概念：46个人物、20种斗技或功法、15种异火、15种丹药、26个势力、18个地点和10种物品或体质。

每个概念生成5至7种问法，包括直接分类、完整类别判断、肯定回答、错误类别纠正、多项选择和一句话解释。同一概念的所有问法固定进入同一个集合，防止同义问题跨训练与验证集合泄漏。1000个问题全部唯一，新增150个概念分别绑定150个不同原文行，最长序列51个字符级Token，词表仍为4483。

构造过程中曾生成一版900条原文上下文题，标签虽可验证，但质量抽样发现OCR错字和截断句，因此该方案被淘汰并保存在里程碑目录作为失败材料，没有进入最终张量。最终数据不复制长篇原文上下文，避免把这些错误引入问题。全项目48项测试通过；下一步从同一个SFT前初始化checkpoint重新训练，先跑20步，再比较事实题、分类题和EOS停止率。

## 5.32 完成1000条数据的20步SFT安全试跑

训练脚本新增可配置的数据、初始化checkpoint、最佳/最终checkpoint、报告、阶段和里程碑路径，默认值保持旧实验兼容。本次明确加载800条训练和100条验证数据，从统一SFT前checkpoint训练20步；100条测试数据没有参与。

训练Loss由5.1175降至1.7259，验证Loss由5.2546降至2.0471，第5、10、15和20步均刷新最佳模型。两个监控回答都主动生成EOS，且checkpoint重新加载后元数据分别确认20步和测试参与数0。安全闭环通过后，正式训练仍从统一SFT前checkpoint重新开始，避免混入试跑更新。

## 5.33 完成1000条数据的800步SFT与正式评估

使用Batch 4、学习率 `1e-4`、Weight Decay `0.01`和梯度裁剪 `1.0`训练800步，约等于4个epoch。验证Loss在第300步达到最低1.2351；其后训练Loss继续由0.4354降到0.1295，但第800步验证Loss回升至1.2891，显示轻微过拟合。最佳第300步与最后第800步分别保存，没有互相覆盖。

模型选择完成后，第300步最佳模型一次性评估100条保留测试：Exact Match 26/100、修正后的答案包含26/100、EOS主动停止100/100。原5道事实题完全正确率0/5；固定10个开放问题显示SFT前模型以小说续写为主，SFT后虽然会短答和停止，却经常把任何问题回答成“人物”“势力”或“异火”。这暴露出900条分类变体压倒100条事实问答造成的分类偏置。

本阶段因此不把训练Loss下降包装成问答成功。结论是停止继续训练当前数据，下一轮减少重复分类题，增加事实、关系、解释、一般指令和不知道/拒答样本。训练报告、完整逐题评估、固定10题表格、Loss CSV、PNG/SVG曲线和审查结论均归档在M003f，供后续视频展示成功项与失败模式。

## 5.34 构造平衡版SFT v3数据集

为修复M003f的分类偏置，重新构造1000条SFT数据并保持800训练、100验证和100测试，便于与上一轮公平比较。新组成是200条原文事实问答、150条事实答案核验、150条概念身份、200条明确复制与比较指令、150条日常对话和150条资料不足回答；概念分类占比从上一版新增数据的100%降至全数据的15%。

数据包含350个独立主题和450种不同答案。200条事实问答来自100个知识主题的原始问法与直接回答改写，明确记录为200条监督样本而不是200个新知识；150条对话覆盖50个人工意图；150条未知题围绕50个经程序确认不在授权语料中的虚构实体。最高频答案“现有资料不足，无法确定。”占15%，已在质量报告中披露可能造成过度拒答。

构建审计逐条验证原文证据、正负事实标签、原样复制、相同与不同文本关系、未知实体缺席、词表字符和主题隔离。比较题修正了初版训练集只有“相同”标签的问题，最终训练为20/20、验证为3/2、测试为5/5。固定10题与数据精确重合为0，最长序列74 Token，词表仍为4483，测试集尚未使用。下一步必须从统一SFT前checkpoint先跑20步，不能接着旧SFT模型训练。

## 5.35 完成平衡版SFT v3的20步安全试跑

从统一SFT前checkpoint使用800条平衡训练数据更新20步，Batch 4、学习率 `1e-4`、Weight Decay `0.01`和梯度裁剪 `1.0`。训练Loss由5.6150降至3.2161，验证Loss由6.2076降至4.5047，第5、10、15和20步均刷新最佳模型；梯度、EOS、保存和重新加载正常，测试参与数为0。

## 5.36 完成平衡版SFT v3的800步训练与公平评估

正式训练再次从统一SFT前checkpoint开始，未接续安全试跑或旧分类模型。验证Loss在第400步达到最低3.4099，之后第500至800步逐渐回升到3.5379，而训练Loss继续下降到0.1630，因此选择第400步作为最佳模型并保留第800步作为过拟合对照。

使用同一套v3测试、相同Temperature、Top-K、长度和逐题随机种子比较旧分类版第300步和平衡版第400步：旧版Exact Match为0/100，平衡版为38/100，两者EOS停止均为100/100。平衡版分项为事实0/10、核验9/15、概念1/15、明确指令8/25、对话0/15、未知20/20。总体提升主要来自未知模板与较简单的核验任务，不能证明开放事实问答或日常聊天已经可用。

固定10题显示平衡版消除了“万物分类”的主要偏置，但答案仍会拼接不相关训练片段。至此字符级SFT v1教学实验完成：数据分布能显著改变行为，但继续增加Step无法克服字符级Token效率、710万参数容量和有限独立知识覆盖。下一阶段进入BPE Tokenizer路线，字符级模型作为完整对照组保留。

## 5.37 完成手写BPE、完整语料编码与10000步预训练

从原4478个字符Token起步，手写基于相邻频次和有序合并规则的BPE，不依赖现成Tokenizer库。从均匀分布的749952字符样本学习2000条合并规则，得到6478词表；再逐行编码完整6544278字符语料。最终Token数为4067842，减少37.84%，平均每Token表示1.609字符，全文解码与原文逐字符一致。

使用Block 256、Batch 4、Embedding 256、8头、6层和8123214参数从零训练10000步。验证Loss由第0步8.8201降至第10000步4.8036，最终步同时是最佳步；训练耗时2925.7秒。固定10题显示输出由随机乱码发展为局部连贯小说，但仍把问题当作续写开头，证明BPE改善表示效率，不会自动产生指令遵循。

## 5.38 完成BPE版平衡SFT与字符版公平评测

将同一1000条平衡SFT数据改用BPE编码，保持800/100/100切分和六类比例不变。扩展5个聊天特殊Token后，逐项确认全部预训练权重精确复制；20步冒烟测试通过，再从同一初始点正式训练800步。验证Loss在第400步达到最低3.7711，800步回升至3.9441，因此选择400步最佳并保留800步过拟合对照。

在模型选择后首次使用100条保留测试，并用同一问题、Temperature、Top-K、随机种子和字符输出上限比较：字符版精确38/100、包含41/100；BPE版精确36/100、包含42/100；两者EOS均100/100。BPE版在明确指令包含率上有小幅收益，但事实问答和日常对话仍为0，严格正确率没有超过字符版。结论是BPE成功解决Token效率和上下文覆盖问题，但当前回答质量主要受812万参数容量、独立知识覆盖和SFT数据质量限制。

## 5.39 制定单语料云训练容量方案

基于6544278字符、4067842个BPE Token和现有语料审计结果，区分了“显存能容纳的模型”与“单本小说能够支撑的模型”。取消原3211万参数正式方案，确定约824万参数为实用上限，并设计8层、Embedding 256、8头、上下文1024、权重共享的最大模型配置。

建立 `CLOUD_TRAINING_PLAN.md`，记录语料v4、3000条SFT v4、预训练/SFT步数、A10卡型、6～12小时运行窗口以及睡前一次验收后可无人值守的checkpoint、早停、GPU监控和自动归档要求。本阶段只完成方案和只读审计，尚未修改训练器或开始租卡。

## 5.40 冻结v4语料并生成带EOS的BPE张量

完成113组章节版本审核，修复29行可核验的异常标题，删除1段SHA锁定的跨章旧稿与14个非正文块；初始26个编号缺口最终核实为9个源文件无独立标题的缺口。按章节组生成90/5/5分片，只用train统计学习2000条BPE合并规则，并验证train/val/test逐字还原以及1599/92/84个EOS与章节数完全一致。

## 5.41 启动本机810万参数正式预训练

在M4 16GB上采用Context 512、Micro Batch 2和梯度累积4完成3步MPS冒烟及断点恢复测试，模型参数量为8105025。正式3000步训练已从随机权重启动，每100步验证并保存最佳/最新检查点，每500步保留固定10题的最多30字生成样本。

## 5.42 完成本机v4正式预训练并归档SFT前基线

正式训练在Apple M4的MPS上完成3000步，耗时2201.6秒。验证Loss从Step 100的7.2220下降到Step 2600的最低5.0447；最后Step 3000训练/验证Loss为4.9333/5.0767，因此按验证集选择Step 2600而不是最后一步。所选最佳模型在测试集20个固定Batch上的Loss为4.9958，checkpoint校验、重新加载和独立评估全部通过。

固定10题每500步保存一次，训练后模型已能生成局部小说叙述和对话，但仍把问题当作续写开头，不具备可靠问答能力。这一失败边界与全部60组阶段样本、最佳模型10题、Loss CSV、PNG/SVG曲线、语料和Token清单、运行配置及SHA-256已归档为M006，作为后续v4 SFT前基线。

## 5.43 低学习率续训至6000步并建立全局评测Harness

从M006由验证集选出的Step 2600权重初始化模型，不继承旧AdamW状态，以最大学习率`5e-5`、最低学习率`1e-5`、Micro Batch 2、梯度累积4续训到累计6000步。结构仍为Embedding 256、8层、8头、FFN 1024、Context 512和8105025参数。第二阶段耗时2111.8秒，两阶段累计4057.5秒、处理24576000个训练Token，约为训练集7.19遍。

最佳验证Loss在Step 6000降至4.7759，相比M006的5.0447降低0.2687（5.33%）；独立固定随机种子的20个Test Batch为4.7476。Checkpoint SHA-256校验与重新加载通过。

新增固定5个小说开头的评测Harness，不允许仅凭Loss宣布模型更好。Harness检查输出长度、中文比例、4-gram重复、最长单字连写和与训练语料的最长逐字重合；Validation Loss负责筛选，自动门槛可以否决，语义连贯性必须人工复核。独立重新加载的Step 6000发布候选虽然Loss最低，却出现连续12个“试”，因此自动状态为`REVIEW`；样本还存在语法不自然、人物关系错乱和情节跳跃。本阶段结论只能是“Loss继续改善”，不能宣布综合生成质量改善。参数、曲线、40组固定样本、门槛报告和哈希归档为M007。

另用Step 6000最佳权重重新运行天气、写作指令、作品事实、概括、日常表达和推测等固定10题，每题最多生成30字。10题均未被直接回答，仍被模型当成小说前缀续写；完整JSON已作为v4正式SFT前行为基线加入M007。
