# 《斗破苍穹》单语料最大模型与云训练方案

> 制定日期：2026-08-28  
> 目标：只使用已获授权的《斗破苍穹》作为预训练知识来源，确定数据能够承受的最大手写 GPT，并设计可在单张云 GPU 上无人值守完成的预训练、SFT、评估和归档流程。  
> 重要边界：这里的“最大”按数据泛化上限判断，不按显存能够塞下多少参数判断。

## 1. 最终结论

| 项目 | 决定 |
|---|---|
| 模型定位 | 《斗破苍穹》文风续写与领域问答实验模型，不是通用聊天模型 |
| 最大实用参数量 | 约 824 万参数 |
| 模型结构 | 8 层 Decoder-only Transformer，Embedding 256，8 头，Head Dim 32 |
| 上下文 | 1024 BPE Token，约覆盖 1500～1650 个中文字符 |
| 预训练 Batch | 8，每步处理 8192 Token |
| 预训练量 | 目标约 4 个等效 epoch，预计 1700～2000 步；最多 6 个 epoch，约 2600～3000 步 |
| 正式 SFT 数据 | 提前准备 3000 条高质量数据，切分为 2400/300/300 |
| 正式 SFT 训练 | 2～3 个 epoch，Batch 8，预计 600～900 步；最多 1200 步 |
| 推荐 GPU | 单张 NVIDIA A10 24GB，即 `ml.gni2.3xlarge` |
| 纯训练时间估计 | 预训练约 1～3 小时，SFT 与评估约 1～2 小时 |
| 完整租用窗口 | 准备充分后约 6～8 小时；首次无人值守运行建议预留 12 小时或直接租 1 天 |

不采用 3200 万参数。A10 能装下 3200 万参数，不代表一本小说能提供足够的信息训练它。3200 万参数按约 20 Token/参数的常见计算最优启发式需要约 6.4 亿多样化训练 Token，是当前训练语料的约 175 倍。

## 2. 当前语料能够提供多少信息

### 2.1 已有规模

| 指标 | 当前实测值 |
|---|---:|
| Stage 3 清洗后字符数 | 6,544,278 |
| 当前 BPE Token 总数 | 4,067,842 |
| 当前预训练 Token | 3,661,057 |
| 当前验证 Token | 406,785 |
| 基础字符词表 | 4,478 |
| BPE 合并数 | 2,000 |
| 当前 BPE 词表 | 6,478 |
| 平均字符/Token | 1.609 |

如果直接按训练集 366 万个不同位置计算，现有 812 万参数模型只有约 0.45 个唯一训练 Token/参数，已经明显处于数据饥饿状态。重复训练可以帮助模型更充分吸收已有模式，但不会创造新人物、情节、语言现象或世界知识。

### 2.2 仍需在正式训练前解决的语料问题

当前 Stage 3 已经可用于教学实验，但还不应直接作为昂贵云训练的最终底稿：

- 仍有 101 个精确重复正文块，估计包含约 108,816 个冗余字符。
- 仍有 113 对相似章节版本待复核，其中 112 对章节号相同。
- 存在 1,686 行额外重复章节标题。
- 保守规则仍命中约 1,692 行作者求票、站点提示等噪声。
- 仍有私用区字符、DEL 控制字符和少量乱码式拼音替换。
- 当前在整条 Token 序列的 90% 位置直接切分，切分点落在章节中间；相似章节版本还可能跨训练集和验证集泄漏。
- 当前 BPE 合并规则先看了全书，再切训练与验证；有若干词项只在验证集出现，输入 Embedding 没有得到预训练正样本。

预计完成最终去重后，BPE 总量约为 380～400 万 Token。精确数字必须在 v4 语料冻结并重新分词后计算。

### 2.3 最终数据处理顺序

1. 人工处理 113 对相似章节版本，并清理已知重复块、重复标题和噪声。
2. 修正章节顺序，将完整章节作为最小分组单位。
3. 按章节和剧情阶段分层切分为 90% 训练、5% 验证、5% 最终测试。
4. 同一章节的所有版本必须处在同一个集合，禁止跨集合泄漏。
5. 只用训练章节学习 2000 条 BPE 合并规则；不再让验证集参与合并规则学习。
6. 预留 `<UNK>`、`<BOS>`、`<USER>`、`<ASSISTANT>`、`<EOS>`、`<PAD>`。
7. 对完整 train/val/test 和全部 SFT 文本做编码往返、OOV、长度与 SHA-256 校验。
8. 冻结语料、Tokenizer 和切分；此后不得在云端临时改变 Token ID。

## 3. 最大实用模型结构

### 3.1 推荐结构

| 超参数 | 建议值 | 原因 |
|---|---:|---|
| Tokenizer | 字符起步 BPE | 保留手搓路线，缩短中文序列 |
| BPE merges | 2000 | 延续现有压缩率；在训练章节上重新学习 |
| 预估词表 | 6484 左右 | 6478 基础/BPE词项，加6个特殊Token |
| `n_embd` | 256 | 与现有模型一致，避免宽度继续扩大造成数据饥饿 |
| `n_layer` | 8 | 通过权重共享节省词表参数后，把容量用于上下文建模 |
| `n_head` | 8 | 每头维度为32，已在现有项目中验证 |
| `head_dim` | 32 | `256 / 8` |
| FFN hidden | 1024 | 标准 4 倍扩张 |
| `block_size` | 1024 | 约覆盖半章文本，比当前256显著增加长程上下文 |
| `batch_size` | 8 | 每步8192 Token，在24GB显存上保留安全余量 |
| Dropout | 0.1 | 单书小数据需要抑制过拟合 |
| 架构 | Pre-Norm、残差、因果注意力 | 延续当前手写 GPT |
| Weight tying | 开启 | 输入 Embedding 与输出分类权重共享 |
| Position Embedding | 可学习绝对位置编码 | 与当前实现一致，长度扩到1024 |
| 激活函数 | GELU | 与现有 Feed Forward 一致 |
| LayerNorm epsilon | `1e-5` | 使用 PyTorch 稳定默认值 |
| Q/K/V bias | 关闭 | 保持现有注意力实现 |
| 输出 projection bias | 开启 | 已包含在 Block 参数量中 |
| 输出 head bias | 开启 | 已单列6484个参数 |
| 参数初始化 | 权重 `Normal(0, 0.02)`；bias 为0 | 所有正式实验固定同一初始化协议 |
| 训练参数精度 | FP32 主参数 + BF16 autocast | 兼顾 AdamW 稳定性与 A10 吞吐、显存 |
| Gradient checkpointing | 默认关闭 | 24GB应能容纳；峰值超过80%时才启用 |

### 3.2 参数组成

按词表 6484 估算：

| 模块 | 参数量 |
|---|---:|
| 共享 Token Embedding/输出权重 | 1,659,904 |
| Position Embedding | 262,144 |
| 8 个 Transformer Block | 6,311,936 |
| 最终 LayerNorm | 512 |
| 输出 Bias | 6,484 |
| **总计** | **8,240,980** |

最终词表每增加或减少一个 Token，总参数约变化 257 个，因此 v4 Tokenizer 冻结后的精确总数可能在 824 万附近小幅变化。

Weight tying 的目的不是单纯压缩数字。当前 812 万模型的输入 Embedding 和独立输出权重约占 332 万参数，即 40.9%；共享后可以把参数更多地用于 Transformer Block，同时避免在同一本小语料上重复学习两套词表矩阵。

### 3.3 运行时完整冻结配置

| 类别 | 参数 | 正式值 |
|---|---|---:|
| 模型 | `vocab_size` | v4 Tokenizer 冻结后实测，预计6484 |
| 模型 | `n_embd / n_layer / n_head` | `256 / 8 / 8` |
| 模型 | `block_size` | 1024 |
| 模型 | `ffn_multiplier` | 4 |
| 模型 | attention / residual / FFN dropout | `0.1 / 0.1 / 0.1` |
| 预训练 | micro batch | 8 |
| 预训练 | gradient accumulation | 1 |
| 预训练 | 每次更新有效Token | 8192 |
| 显存回退 | micro batch / accumulation | `4 / 2`，有效Token仍为8192 |
| 数据 | train / val / test | 按完整章节 `90% / 5% / 5%` |
| 验证 | 预训练验证数据 | 每100步顺序遍历完整val一次，不随机抽样 |
| 保存 | checkpoint间隔 | 每100步，另存best/latest/rolling×2 |
| 加载 | workers / pinned memory | `2 / True`；冒烟后按GPU等待率再调至4 |
| SFT | max sequence length | 512；超长记录在租卡前拦截，不静默截断 |
| SFT | padding label | `-100`，问题和PAD不参与Loss |
| SFT | micro batch / accumulation | `8 / 1` |
| SFT验证 | 300条验证集 | 每75步完整评估 |
| 生成评测 | 解码 | 同时保存greedy与 `temperature=0.8, top_k=50` |
| 生成评测 | 停止 | 生成`<EOS>`或达到256个新BPE Token |
| 可复现 | seed | 42；保存CPU/CUDA/采样器RNG状态 |
| 编译 | `torch.compile` | 首次正式运行关闭；只在单独基准通过后开启 |

配置必须写进一个版本化配置文件并保存 SHA-256；正式运行不再依赖散落的环境变量默认值。这里的“显存回退”只改变一次送进GPU的样本数，不改变有效 Batch，也不改变目标训练Token量。

### 3.4 为什么这就是最大实用规模

| 模型规模 | 判断 |
|---:|---|
| 430～530 万 | 更保守、验证泛化风险更低 |
| **约 824 万** | **单书数据的实用上限，本方案采用** |
| 约 1580 万 | 仅适合作为教学 A/B 对照，预计记忆和过拟合明显增加 |
| 约 3210 万 | 数据不足，不进入正式训练 |

严格按大模型常见的数据/参数比例计算，这本小说甚至不足以数据最优地训练数百万参数。本方案仍选择约824万，是为了在中文词表开销、现有工程结构、上下文能力与可观察效果之间取得“最大可接受”折中，而不是声称它满足大模型缩放定律。

## 4. 预训练方案

### 4.1 优化器与训练参数

| 项目 | 设置 |
|---|---|
| Optimizer | AdamW |
| Betas | `(0.9, 0.95)` |
| Epsilon | `1e-8` |
| 峰值学习率 | `3e-4` |
| Warmup | 100 步 |
| Scheduler | Cosine decay |
| 最低学习率 | `3e-5` |
| Weight decay | `0.1` |
| Gradient clip | `1.0` |
| Precision | 优先 BF16；不支持时使用 FP16 + GradScaler |
| Seed | 42 |
| 验证间隔 | 每100步，使用固定验证窗口 |
| Checkpoint | 每100步保存 latest；同时保存 best 和最近2份 rolling |
| Early stopping | 连续5次验证无实质改善即停止 |

### 4.2 训练 Token 与步数

设最终 v4 训练集为 `N_train` 个 BPE Token：

- 每步 Token：`8 × 1024 = 8192`
- 目标训练量：`4 × N_train`
- 目标步数：`ceil(4 × N_train / 8192)`
- 最大训练量：`6 × N_train`
- 最大步数：`ceil(6 × N_train / 8192)`

如果最终训练集约为 350～400 万 Token：

| 训练量 | 预计步数 | 用途 |
|---|---:|---|
| 100步 | 100 | CUDA、显存、Loss、保存与恢复冒烟测试 |
| 4个等效epoch | 1700～2000 | 正式目标 |
| 6个等效epoch | 2600～3000 | 仅当验证Loss仍改善时允许达到的上限 |

不能再机械沿用10000步。新方案每步处理8192 Token，是旧方案每步1024 Token的8倍；1800步已经约等于1475万个训练位置。

## 5. SFT 方案

### 5.1 必须提前准备

SFT 数据必须在租 GPU 之前完成构造、审核、分割、编码和哈希冻结。云端只负责训练，不应把付费时间花在生成问题、人工核对答案或临时修复 OOV 上。

当前1000条数据只适合作为历史对照：

- 只有350个独立主题。
- 回答侧总计只有8645个BPE监督Token，平均8.6 Token/条。
- 35.5%样本集中在5个重复答案模板。
- BPE最佳测试虽然精确36/100，但原文事实为0/10、日常对话为0/15；其中19分来自统一的“资料不足”模板。

### 5.2 正式 SFT v4

目标为3000条，切分2400训练、300验证、300隐藏测试。所有知识都从授权小说及其结构化证据中取得，不增加新的预训练语料。

| 类型 | 数量 |
|---|---:|
| 直接事实、实体和世界设定 | 750 |
| 人物关系、因果、动机和时间线 | 600 |
| 带原文片段的理解、摘要和抽取 | 450 |
| 续写、改写、风格和明确指令 | 450 |
| 事实核验与纠错 | 300 |
| 信息不足、时间位置歧义和澄清 | 300 |
| 基础对话和聊天控制 | 150 |

质量门槛：

- 至少1200个独立主题；同一事实最多两种问法。
- 至少70%的记录带章节、原文跨度、证据和语料哈希。
- 平均回答30～60个BPE Token，不能全部是极短分类答案。
- 任一完全相同答案不得超过总数据的2%。
- 按主题、场景或章节分组切分，禁止同义题跨集合泄漏。
- 300条验证和300条测试全部人工审核；训练数据100%自动校验并分层抽审至少20%。
- 当前历史10题只作回归集；另建50条固定提示和300条从未看过的最终测试。

### 5.3 SFT 训练参数

| 项目 | 设置 |
|---|---|
| 初始化 | 预训练 best checkpoint，不使用 last checkpoint |
| Batch | 8 |
| 学习率 | `5e-5` |
| Warmup | 总步数的5% |
| Scheduler | Cosine decay 至约 `5e-6` |
| Weight decay | `0.01` |
| Gradient clip | `1.0` |
| Precision | BF16 |
| 目标训练量 | 2～3 epoch，即600～900步 |
| 最大训练量 | 4 epoch，即1200步 |
| 验证 | 每75步 |
| Early stopping | 连续4次无实质改善即停止 |

正式训练前先完成20～50步SFT冒烟测试，然后丢弃冒烟权重，从同一个预训练best checkpoint重新开始正式SFT。

## 6. GPU 与时间需求

### 6.1 卡型

推荐单张 NVIDIA A10 24GB，对应火山机器学习平台 `ml.gni2.3xlarge`。

- 约824万参数本身很小，常驻模型、梯度和Adam状态只占少量显存。
- 主要显存来自 `Batch 8 × Block 1024 × 8 Heads × 8 Layers` 的注意力和Logits激活。
- BF16下预计峰值显存约8～16GB；必须以100步实测为准。
- 16GB卡可能也能运行，但安全余量不足；24GB A10更适合首次无人值守。
- A30或A100能够运行，但对这个小型手写模型不会带来与价格或峰值算力成比例的加速，瓶颈很可能是许多小矩阵操作和Python层多头实现。

### 6.2 显存、算力和磁盘需求

| 资源 | 估算需求 | 说明 |
|---|---:|---|
| 模型参数 | 824.1万 | v4词表冻结后会有小幅变化 |
| FP32参数+梯度+Adam状态 | 约132MB | 粗按每参数16字节估算 |
| 训练峰值显存 | 约8～16GB | 主要来自1024上下文的注意力和激活，不是模型权重 |
| 安全显存 | 24GB | 要求100步实测峰值不超过19.2GB |
| 预训练总Token | 约1400～1600万 | 最终训练集的4个等效epoch |
| 预训练计算量 | 约1 PFLOP量级 | 含长上下文注意力的数量级估算，不用于计费 |
| 主机内存 | 32GB以上，推荐56GB | 数据很小，主要给Python、缓存和构建留余量 |
| 可用磁盘 | 至少50GB，推荐100GB | 容纳环境、原始/处理数据、轮转checkpoint、日志和归档 |
| 外部持久存储 | 至少10GB | 保存best/latest、报告、哈希和失败现场 |

仅从显存看，824万参数远小于A10容量；选择24GB是为了容纳 `Batch 8 × Context 1024` 的中间激活，并留出故障排查余量。若100步冒烟峰值超过80%，先切换为 `micro_batch=4 + accumulation=2`，而不是缩短上下文或临时改模型。

### 6.3 时间估计

| 阶段 | 预计时间 |
|---|---:|
| 环境、上传与CUDA检查 | 0.5～1小时 |
| 100步端到端冒烟、SIGTERM和resume演练 | 0.5～1小时 |
| 预训练1700～2000步 | 1～3小时 |
| SFT冒烟与正式600～900步 | 0.5～1小时 |
| 固定评估、曲线、打包与外部备份 | 0.5～1小时 |
| 异常重试与时间缓冲 | 2～4小时 |
| **完整建议窗口** | **6～8小时；首次运行预留12小时** |

第一轮100步结束后，用实测中位数计算剩余时间：

`预计小时 = 剩余步数 × 秒/步 × 1.25 / 3600`

系数1.25用于验证、生成、保存和速度波动。

以2000步预训练为例，含1.25系数后的敏感性计算如下：

| 100步实测中位速度 | 2000步预训练预计墙钟时间 |
|---:|---:|
| 0.5秒/步 | 约0.35小时 |
| 1秒/步 | 约0.69小时 |
| 2秒/步 | 约1.39小时 |
| 4秒/步 | 约2.78小时 |

因此“预训练1～3小时”不是保证值，而是A10上目标实现的验收区间。若冒烟测试慢于4秒/步，先检查GPU利用率、数据等待和逐头Python循环，再决定是否正式开跑；不能靠盲目延长租用时间掩盖实现问题。

## 7. 无人值守流程

当前代码只能做有人观察的实验，不能直接启动后睡觉。租卡前必须补齐下面的自动状态机：

`PRECHECK → SMOKE → RESUME_TEST → PRETRAIN → PRE_SFT_BASELINE → SFT_SMOKE → SFT → EVALUATE → ARCHIVE → DONE`

### 7.1 睡前只检查一次

100步冒烟完成后，只有同时满足以下条件才自动进入正式训练：

- `torch.cuda.is_available()` 为真，并确认GPU型号是预期卡型。
- 模型、输入、标签和生成函数全部位于同一个CUDA设备。
- BF16前向、Loss、反向传播和Optimizer更新成功。
- Loss与梯度均为有限值，没有NaN/Inf。
- 峰值显存不超过卡容量的80%。
- latest checkpoint能够重新加载。
- 人为终止任务后能从latest恢复并继续至少20步。
- 验证Loss相较step 0有下降。

这次检查通过后即可让任务整晚自行运行，不需要每小时查看。

### 7.2 必须实现的安全能力

- CUDA不可用时立即失败，禁止悄悄回退CPU。
- 修复SFT训练使用CUDA、监控生成却默认使用CPU的设备不一致问题。
- 真正恢复model、optimizer、step、best、RNG、batch generator和AMP scaler。
- 临时文件写入、重新加载校验后再原子替换checkpoint。
- 保存best、latest、最近两份rolling和emergency checkpoint。
- 处理SIGTERM/SIGINT、CUDA OOM、NaN/Inf和磁盘不足。
- 设置最大墙钟时间，并给评估和外部备份预留至少2小时。
- SSH断开后训练继续；训练进程由独立任务或会话管理器托管。
- 平台侧另设最长任务时限或自动释放，不能只依赖Python进程。

### 7.3 分模块日志与诊断

| 日志 | 内容 |
|---|---|
| `preflight.log` | CUDA、驱动、PyTorch、配置、数据哈希、磁盘 |
| `data.log` | 语料、Tokenizer、切分、OOV和张量加载 |
| `pretrain.log` | Step、Loss、学习率、梯度、tokens/s、ETA |
| `validation.log` | 固定窗口Loss、最佳点和早停计数 |
| `checkpoint.log` | 保存、校验、恢复、同步和异常checkpoint |
| `gpu.log` | 每30～60秒记录利用率、显存、温度和功耗 |
| `sft.log` | SFT Loss、监督Token、epoch和EOS指标 |
| `orchestrator.log` | 各阶段开始/完成/失败、run ID和退出码 |

每类日志必须能单独调节级别和过滤。不得记录密码、Token、密钥、完整授权头或不必要的个人数据。默认轮转，建议每类10～20MB、保留5份。

### 7.4 最终产物

无人值守任务完成后必须自动生成：

- 预训练best/latest checkpoint及SHA-256。
- SFT前固定样本和SFT后固定样本。
- SFT best/latest checkpoint及SHA-256。
- 预训练和SFT Loss CSV、PNG/SVG曲线。
- 预训练验证指标、SFT六类分项指标和人工抽样表。
- GPU利用率、峰值显存、tokens/s和总耗时报告。
- `DONE.json`或`FAILED.json`，写明停止原因和最后可恢复checkpoint。
- 完整manifest与压缩归档，并同步到实例外的持久存储。

## 8. 执行顺序与开租条件

### 8.1 正确顺序

1. 完成语料v4清理与章节级切分。
2. 在训练章节上重学BPE并冻结Tokenizer。
3. 构造并审核3000条SFT v4，生成固定验证和隐藏测试。
4. 修改训练器：weight tying、Dropout、1024上下文、AMP、resume、早停和原子checkpoint。
5. 完成CPU级单元测试和20步流程测试。
6. 打包代码、数据、配置和哈希；不上传Mac本地`.venv`。
7. 租A10，完成100步CUDA与故障恢复验收。
8. 验收通过后，启动最多12小时的自动流水线。

### 8.2 开租门槛

以下任意一项未完成，就不应开始计费：

- 最终语料和Tokenizer仍可能改变。
- SFT 3000条尚未完成质量验收。
- CUDA设备检查仍允许回退CPU。
- checkpoint不能恢复完整训练状态。
- 没有早停、墙钟上限或平台自动释放。
- 没有实例外checkpoint备份。
- 没有演练SSH断开、SIGTERM和resume。

## 9. 预期效果与建议

### 9.1 可以期待

- 比当前模型覆盖更长上下文，小说段落结构和角色语气更稳定。
- 预训练后具备更好的《斗破苍穹》风格续写。
- SFT后能遵循基本问答格式、正确停止，并改善经过训练覆盖的领域事实和解释任务。
- 完整保留预训练前后、SFT前后、失败恢复和GPU训练的可复现视频材料。

### 9.2 不能期待

- 不能成为通用GPT或替代ChatGPT。
- 不能因为参数增加就自动理解天气、编程、数学和现实世界知识。
- 不能仅靠反复训练一本书获得书外知识。
- SFT可以改变回答行为，但不能弥补预训练知识范围之外的能力。

### 9.3 最重要建议

本项目下一步不是先租卡，而是先完成“语料v4 + SFT v4 + 无人值守训练器”。在这些产物冻结前租GPU，只会把付费时间浪费在清洗、调试和人工值守上。

## 10. 估算依据与边界

- DeepMind 的 Chinchilla 工作说明，在计算最优训练中模型规模与训练Token应同步扩大；70B模型使用约1.4T Token，对应约20 Token/参数。该规律来自大规模模型，本方案只把它作为判断“3200万明显过大”的数量级证据，不机械当作微型中文模型的精确公式：<https://arxiv.org/abs/2203.15556>
- 数据受限语言模型研究表明，重复数据在少量epoch内仍有价值，但继续高次数重复的收益递减，因此本方案把4个等效epoch设为目标、6个设为上限：<https://arxiv.org/abs/2305.16264>
- 输入Embedding与输出权重共享可减少重复词表参数，本方案据此将节省的容量转入Transformer Block：<https://aclanthology.org/E17-2025/>
- 火山机器学习平台规格页显示 `ml.gni2.3xlarge` 配置单张NVIDIA A10 24GB；最终卡型仍需在下单页复核区域库存与镜像兼容性：<https://www.volcengine.com/docs/6419/1330945?lang=zh>

所有时长都是工程估算，真正决定值来自目标镜像、目标代码和目标配置的100步实测。方案使用25%的时间开销系数、显存不超过80%的验收线，以及2～4小时故障缓冲；这些余量用于首次无人值守运行，不代表必须把卡跑满。
