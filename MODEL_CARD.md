# Model Card：Doupo GPT v4

> 状态：v4 预训练完成，SFT 尚未开始。以下预训练数字来自 M006 正式运行；SFT 字段将在下一阶段补充。

## 模型简介

Doupo GPT v4 是一个用于学习 GPT 原理的中文 Decoder-only Transformer。它只使用已获授权的《斗破苍穹》作为预训练知识来源，随后使用具有语料证据的领域指令数据进行 SFT。

它的目标是展示 Tokenizer、预训练、SFT、验证、生成和无人值守训练的完整链路，不是通用聊天模型，也不是原作品或成熟商业大模型的替代品。

## 架构

| 项目 | 配置 |
|---|---:|
| 参数量 | 8,105,025 |
| Transformer 层数 | 8 |
| Embedding | 256 |
| Attention Heads | 8 |
| Head Dimension | 32 |
| FFN Hidden | 1024 |
| Context Length | 512 BPE Token |
| Tokenizer | 字符起步 BPE，训练集学习2000次合并 |
| Weight Tying | 开启 |

## 数据

- 预训练来源：已获授权的《斗破苍穹》文本。
- 原始语料、清洗全文和可还原全文的 Token 张量不随源码仓库发布。
- 语料按完整章节分组为 train/validation/test；同章版本不得跨集合。
- BPE 合并规则只能从训练章节学习。
- SFT v4 尚未冻结；验证和测试记录必须全部审核，主题不得跨集合泄漏。
- 预训练模型、Tokenizer 和语料清单的 SHA-256 已记录在 M006；原始语料与可逆 Token 张量不公开。

## 预期用途

- 学习小型 GPT 的结构与训练过程。
- 在授权范围内进行小说风格续写实验。
- 进行作品内部人物、设定、关系和情节问答实验。
- 对比预训练前后、SFT 前后以及不同 Tokenizer 的行为变化。

## 不适用场景

- 通用知识问答、实时天气、新闻、编程或数学助手。
- 医疗、法律、金融等高风险决策。
- 将生成内容冒充原作者或原作正文。
- 未经人工核验直接发布事实性回答。

## 训练与评测

| 项目 | 正式结果 |
|---|---|
| 预训练 Token | Train 3,416,400；Validation 195,068；Test 184,808 |
| 预训练 Step / 约当 Epoch | 3,000 / 约3.60 |
| 最佳 Validation Loss | 5.0447（Step 2,600） |
| 所选模型 Test Loss | 4.9958 |
| SFT Step / Epoch | 待填写 |
| SFT 六类指标 | 待填写 |
| EOS 停止率 | 待填写 |
| 50题固定回归 | 待填写 |
| 300题隐藏测试 | 待填写 |
| 正式训练设备 / 耗时 | Apple M4 MPS / 2,201.6秒 |
| Best Checkpoint SHA-256 | `1cb34b5bbf6cbef13c2f7239f80c8bf5caa4e773ef0967166803cf33e194c559` |

测试结果必须同时报告严格正确率、分项表现和失败样本，不能只展示最好看的生成文本。

## 已知限制

- 单本小说的信息量远小于通用模型训练数据，模型容易记忆、复述或过拟合。
- SFT 能改变输出格式和停止行为，但不能创造书外知识。
- 角色关系和情节随故事阶段变化，脱离时间位置的问题可能没有唯一答案。
- 即使答案流畅，也可能出现人物、关系和事件混淆。
- 生成结果可能包含与原作相似的表达，公开使用前需要人工检查。

## 发布与许可证

- 只发布选定的 best Checkpoint，不发布大量中间或失败权重。
- 模型权重通过 GitHub Release 或 Git LFS 发布，并附带哈希与本文件。
- 代码许可证、模型权重许可证和数据授权说明可能不同；正式发布前必须分别确定。目前许可证尚未选择。
